<Window x:Class="CoWorkingManager.UI.Views.RezervacijeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        Title="Rezervacija"
        Height="400"
        Width="400"
        Background="#F4F6F9">

    <Grid Margin="20">

        <StackPanel>

            <Label Content="Korisnik:"/>
            <ComboBox x:Name="ComboKorisnik" SelectionChanged="ComboKorisnik_SelectionChanged"/>

            <Label Content="Lokacija:"/>
            <ComboBox x:Name="ComboLokacija" SelectionChanged="ComboLokacija_SelectionChanged"/>

            <Label Content="Resurs:"/>
            <ComboBox x:Name="ComboResurs" SelectionChanged="ComboResurs_SelectionChanged"/>

            <Label Content="Datum i vreme početka:"/>
            <DatePicker x:Name="DatePocetak" DisplayDateStart="{Binding Path=Today}"/>

            <Label Content="Datum i vreme završetka:"/>
            <DatePicker x:Name="DateZavrsetak" DisplayDateStart="{Binding Path=Today}"/>

            <TextBlock x:Name="ValidationError" Foreground="Red" Visibility="Collapsed" TextWrapping="Wrap"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 20 0 0">
                <Button x:Name="BtnSacuvaj" Content="Dodaj" Width="100" Margin="5" Click="Dodaj_Click"/>
                <Button x:Name="BtnIzmeni" Content="Izmeni" Width="100" Margin="5" Click="Izmeni_Click"/>
                <Button x:Name="BtnOtkaziRezervaciju" Content="Otkazi" Width="100" Margin="5" Click="Otkazi_Click"/>
                <!-- 
                <Button Content="Zatvori" Width="100" Margin="5" Click="Zatvori_Click"/>
                -->
            </StackPanel>

            <!-- Status -->

            <TextBlock x:Name="Uspesno"
                       Foreground="Green"
                       FontWeight="Bold"
                       Visibility="Collapsed"
                       TextAlignment="Center"
                       Margin="10">
                Akcija uspešno izvršena
            </TextBlock>

            <TextBlock x:Name="Neuspesno"
                       Foreground="Red"
                       FontWeight="Bold"
                       Visibility="Collapsed"
                       TextAlignment="Center">
                Nije moguće izvršiti akciju
            </TextBlock>

        </StackPanel>

    </Grid>

</Window>